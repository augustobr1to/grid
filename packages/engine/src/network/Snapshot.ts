/**
 * Snapshot — serialisation helpers for networking.
 * Types are imported from @thegridcn/shared.
 */
export { type InputSnapshot, type WorldSnapshot, type EntityState, type PlayerInfo, type ReconcilePayload } from '@thegridcn/shared';
